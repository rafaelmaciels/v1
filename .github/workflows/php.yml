name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Validate composer.json and composer.lock
      run: |
        if [ -f composer.json ]; then
          echo "Found composer.json — running composer validate --strict"
          composer validate --strict
        else
          echo "composer.json not found, skipping composer validate"
        fi

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    - name: Install dependencies
      run: |
        if [ -f composer.json ]; then
          echo "Found composer.json — running composer install"
          composer install --prefer-dist --no-progress
        else
          echo "composer.json not found, skipping composer install"
        fi

    # Adicione um script de testes ao composer.json, exemplo: "test": "vendor/bin/phpunit"
    # Docs: https://getcomposer.org/doc/articles/scripts.md

    # - name: Run test suite
    #   run: composer run-script test
